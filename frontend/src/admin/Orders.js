import React from "react";
import Base from "../core/Base";

const Orders = () => {
  return (
    <Base
      title="Add Orders"
      description="Add the type of Orders you want"
    ></Base>
  );
};
export default Orders;
